<!--<script>
    $(document).ready(function () {
        $("#buscar").click(function () {
            var texto = $("#texto").val();
            $.ajax({
                url: 'http://localhost:3000/lista/vehiculos/buscar',
                data: 'texto=' + texto,
                success: function (data, textStatus, jqXHR) {
                    console.log(data);
                    var html = '';
                    $.each(data, function (index, item) {
                        html += '<tr>';
                        html += '<td>' + (index) + '</td>';
                        html += '<td>' + item.nombre_Veh + '</td>';
                        html += '<td>' + item.propietario.apellidos + item.propietario.nombres + '</td>';
                        html += '<td>' + item.color + '</td>';
                        html += '<td>' + item.marcar + '</td>';
                        html += '<td>' + item.placa + '</td>';
                        html += '<td> <a href=""' + item.external_id + 'class="btn btn-primary">Modificar</a></td>'
                        html += '</tr>';
                    });
                    $("#tabla tbody").hbs(html);
                }
            });
        });
    });

</script>-->
<title>{{title}}</title>

<div class="container">
    <h2 class="title">{{title}}</h2>

    <div class="card-body">
        <a class="btn btn-success card-title" href="/">Volver</a>

        <div class="card-body">
            <input id="texto"/>
            <a href="#" id="buscar" class="btn btn-danger">Buscar</a>
        </div>
        <a class="btn btn-success card-title" href="/vehiculo/registro">Nuevo</a>
        <table id="tabla" class="table">
            <thead>
                <tr>
                    <th>Nro</th>
                    <th>Marca</th>
                    <th>Precio</th>
                    <th>Descripcion</th>
                    <th>Cliente</th>
                    <th>Acciones</th>
                </tr>
            </thead>
            <tbody id="cuerpo" align="center">


            </tbody>

            <tbody>
                <tr>
                    <td>
                    </td>
                    <td>
                    </td>
                    <td>
                    </td>
                    <td>
                    </td>


                    <td>
                        <a href="/vehiculo/modificar" class="btn btn-success">Modificar vehiculo</a>
                    </td>
                </tr>
        </table>

    </div>
</div>
